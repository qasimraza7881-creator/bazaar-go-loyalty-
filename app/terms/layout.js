export const metadata = {
  title: "Terms & Conditions",
  description:
    "Bazaar Go Loyalty use karne ke terms aur conditions — subscription, billing aur account usage rules.",
  alternates: { canonical: "/terms" }
};

export default function TermsLayout({ children }) {
  return children;
}
