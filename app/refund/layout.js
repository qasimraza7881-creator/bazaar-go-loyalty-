export const metadata = {
  title: "Refund Policy",
  description:
    "Bazaar Go Loyalty ki refund policy — free trial, billing errors aur cancellation ke rules.",
  alternates: { canonical: "/refund" }
};

export default function RefundLayout({ children }) {
  return children;
}
